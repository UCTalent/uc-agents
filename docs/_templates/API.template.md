# {ModuleName} — API Map

Schema chi tiết: Swagger / OpenAPI / GraphQL schema — **không duplicate ở đây**.

Prefix NestJS: `/api` (global) + module path (thường `/v2/...`).

## REST

| Method | Path | Use Case | Auth | Ghi chú |
|--------|------|----------|------|---------|
| | | | JWT / public | |

## GraphQL

| Operation | Resolver | Use Case | Auth |
|-----------|----------|----------|------|
| | | | |

## Controller / resolver source

- `presentation/http/controllers/` hoặc `presentation/http/controller/`
- `presentation/graphql/resolvers/`
